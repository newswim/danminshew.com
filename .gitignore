# dependencies / build
node_modules/
dist/
.astro/
.vercel/

# environment & secrets
.env
.env.*

# os / editor noise
.DS_Store
.obsidian/workspace*
.obsidian/cache

# private knowledge base — never committed, even while the repo is private.
# Keeps git history clean so the repo can go public later without scrubbing.
docs/private/
.vercel
.env*
